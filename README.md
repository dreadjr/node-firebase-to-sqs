# node-firebase-to-sqs

# release

    # commit and add release tags
    git add .
    git commit -m “Initial release”
    git tag v0.0.1 
    git push origin master --tags
    
    # Publish to npm
    npm publish
